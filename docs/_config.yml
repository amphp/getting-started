kramdown:
  input: GFM
  toc_levels: 2..3

baseurl: "/getting-started"
layouts_dir: ".shared/layout"

exclude: ["Gemfile", "Gemfile.lock", "README.md", "vendor"]
include: [".shared"]

repository: amphp/getting-started
gems:
 - "jekyll-github-metadata"
 - "jekyll-relative-links"

defaults:
 - scope:
     path: ""
     type: "pages"
   values:
     layout: "docs"

asset_path: "/getting-started/.shared/asset"

navigation:
 - tcp-chat
